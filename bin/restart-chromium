#!/usr/bin/env bash

echo "Sending TERM signal to all chromiums"
killall chromium

while pgrep chromium; do
    echo "Waiting for chromium to die..."
    sleep 0.5
done

echo "Starting chromium back up"
exec chromium
