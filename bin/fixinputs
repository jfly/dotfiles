#!/bin/bash

set -e

# https://wiki.archlinux.org/index.php/Mouse_acceleration
xset m 2.5 0

# Turn off capslock just in case it's on.
# Normally, I run fixkeys right after noticing that my capslock key is
# actually doing capslock and not escape. I then run fixkeys, which previously
# would trap me in capslock mode, because capslock no longer disables
# capslock.
capslockx off

# Speed up keyrepeat
xset r rate 200 30

# First reset keys before running xmodmap.
setxkbmap
xmodmap ~/.Xmodmap
